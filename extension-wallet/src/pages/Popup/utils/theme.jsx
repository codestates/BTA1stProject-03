import { createTheme } from '@mui/material'
export const theme = createTheme({
    palette: {
        secondary: {
            // This is green.A700 as hex.
            main: '#6eb5aa',
            contrastText: '#fff',
        },
    },
})
